import {StyleSheet} from 'react-native';

export const buttonStyles = StyleSheet.create({
  mainContainer: {
    width: '80%',
    margin: 5,
    borderWidth: 0.5,
    borderColor: 'white',
    borderRadius: 30,
    overflow: 'hidden',
  },
});
